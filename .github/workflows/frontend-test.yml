name: Frontend Test CI

on:
  push:
    paths:
    - 'bubblemeter/frontend/**'

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node.version: [12.x]
    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - name: Dependency Cache
      uses: actions/cache@v1.1.2
      with:
        path: bubblemeter/frontend/node_modules
        key: frontend_node_modules
    - name: npm install
      run: cd bubblemeter/frontend && npm run install
    - name: npm run test
      run: cd bubblemeter/frontend && npm run test

  lint:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node.version: [12.x]
    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - name: Dependency Cache
      uses: actions/cache@v1.1.2
      with:
        path: bubblemeter/frontend/node_modules
        key: frontend_node_modules
    - name: npm install
      run: cd bubblemeter/frontend && npm install
    - name: npm run lint
      run: cd bubblemeter/frontend && npm run lint